#pragma once

/// @brief Функция корректного считывания значения
/// @return Результат считывания введённого значения
int ReadingCorrectValue();

/// @brief Функция корректного считывания размера
/// @return Результат считывания введённого значения
int ReadingCorrectSize();

/// @brief Функция проверки создания нужной Структуры Данных
/// @param checkMaking Метка создания
/// @return Создан или нет
bool CheckDataStructureMaking(bool checkMaking);
